{% extends 'base.html.twig' %}

{% block title %}{{categ_site.nom}}{% endblock %}

{% block body %}
<br><br><br>
 <div class="content">
    <div class="card">
        <div class="card-body">
            <fieldset class="mb-3">
                <legend class="text-uppercase font-size-sm font-weight-bold">Catégorie de site </legend>

                <div class="form-group row">
                    <label class="col-form-label col-lg-2">Nom de catégorie :</label>
                    <div class="col-lg-10">
                    <div class="form-control">
                    {{categ_site.nom}}
                    </div></div>
                </div>
                <br>
                <a href="{{path('show_categoriesite')}}"><button type="button" class="btn btn-primary">Retourner</button></a>
            </fieldset>
        </div>
</div>
</div>
{% endblock %}